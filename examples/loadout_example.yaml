---
# example loadout for file management for keeper

apps:
  myawesomeapp:
    components:
      mongo:
        managed_files:
          - name: dbpassword.txt
            local_path: etc
            remote_path: /secrets
          - name: install.sh
          - name: start_app.sh
        tasks: # Not implemented yet
          - name: run_file
            params: [install.sh]
          - name: run_file
            params: [start_app.sh]
